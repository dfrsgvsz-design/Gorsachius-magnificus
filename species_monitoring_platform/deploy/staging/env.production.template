# Production env for Gorsachius magnificus survey backend
# Copy to backend/.env on the server
APP_PORT=8000
WEB_CONCURRENCY=1
# Allow APK + frontend dev origins (adjust as needed)
CORS_ORIGINS=https://36.139.152.185,http://localhost:5173,capacitor://localhost,https://localhost
BIRD_API_KEY=
XC_API_KEY=
# Path overrides (optional; defaults are fine for SSH-based install)
BIRD_PLATFORM_RUNTIME_DIR=/opt/gm-backend/runtime
BIRD_PLATFORM_DATA_DIR=/opt/gm-backend/data
BIRD_PLATFORM_OUTPUT_DIR=/opt/gm-backend/output