from setuptools import setup, find_packages

setup(
    name="gorsachius-shared",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Core dependencies shared by both platforms
    ],
    description="Shared modules for Gorsachius magnificus monitoring platforms",
)
