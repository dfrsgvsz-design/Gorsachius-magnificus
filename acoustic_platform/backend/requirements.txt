# Core web framework
fastapi==0.115.0
uvicorn==0.30.0
# Production process manager: gunicorn supervises N uvicorn workers
# (`gunicorn -k uvicorn.workers.UvicornWorker -w 2 main:app`) so the API
# survives crashes inside a single worker and recycles cleanly.
gunicorn==23.0.0
python-multipart==0.0.9
pydantic==2.6.0
aiofiles==23.2.1
websockets==12.0

# Deep learning
torch==2.2.0
torchaudio==2.2.0
torchvision==0.17.0
timm==1.0.3

# Audio processing
librosa==0.10.2
soundfile==0.12.1

# Scientific computing
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.4.0
pandas==2.2.0
matplotlib==3.8.3

# Utilities
requests==2.33.1
Pillow==12.2.0

# Optional: BirdNET baseline engine (install separately if needed)
# birdnetlib==0.16.2

# HTTP client
httpx==0.28.1

# Async SQLite
aiosqlite==0.22.1

# Authentication
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Optional crash / error monitoring (works against either Sentry SaaS or a
# self-hosted GlitchTip instance). Backend only initializes when SENTRY_DSN
# env var is set, so installing the SDK in the image is safe by default.
sentry-sdk==2.18.0

# NOTE: pytest / pytest-asyncio / pytest-cov / ruff / httpx moved to
# requirements-dev.txt (dev-only). Production runtime must not depend on them.
