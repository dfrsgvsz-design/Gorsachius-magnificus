# Quality-gate / dev-only dependencies (NOT shipped in production runtime).
# Runtime dependencies live in requirements.txt. Keep this file slim and stable.
#
# Install order (locally and in CI):
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Tracked by quality_gate.ps1 / release_gate.ps1 step 0.

pytest>=8.0,<9.0
pytest-asyncio>=0.25,<1.0
pytest-cov>=5.0,<7.0
ruff>=0.6,<1.0
# FastAPI TestClient HTTP transport
httpx>=0.27,<1.0
