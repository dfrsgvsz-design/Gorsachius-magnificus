{
  "name": "gorsachius-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Cross-app Playwright Test E2E suite. Targets species_monitoring_platform's 5-step critical flow: app boot → survey protocol selection → checkin → offline submit → reconnect sync.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:species": "playwright test --project=species-chromium",
    "test:report": "playwright show-report",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.4.0"
  }
}
